// BattlesSection - placeholder kept for routing/layout compatibility
// Battle access is now through the Quick Actions menu and Portal page

const BattlesSection = () => {
  return null;
};

export default BattlesSection;
